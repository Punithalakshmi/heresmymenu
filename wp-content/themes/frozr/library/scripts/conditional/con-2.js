	jQuery.noConflict();	jQuery(document).ready(function(){		//add stapel effect to galleries		jQuery('.stapel_gallery').wrapAll('<div id="tp-grid" class="tp-grid" />');		jQuery('#tp-grid').before('<div class="topbar"><span id="close" class="back fs-icon-arrow-left"></span><h3 id="name"></h3></div>');		var grid = jQuery( '#tp-grid' ),			name = jQuery( '#name' ),			close = jQuery( '#close' ),			loader = jQuery( '<div class="loader"><i></i><i></i><i></i><i></i><i></i><i></i><span>Loading...</span></div>' ).insertBefore( grid ),			stapel = grid.stapel( {				onLoad : function() {					loader.remove();				},				onBeforeOpen : function( pileName ) {					name.html( pileName );				},				onAfterOpen : function( pileName ) {					close.show();				}			});			close.on( 'click', function() {				close.hide();				name.empty();				stapel.closePile();			});	});